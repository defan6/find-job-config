spring:
  application:
    name: user-service-ms
  datasource:
    url: jdbc:postgresql://localhost:5433/users
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
jwt:
  secret: 76fda2e1d5b2f21d82b61b8732b85b7876fda2e1d5b2f21d82b61b8732b85b7876fda2e1d5b2f21d82b61b8732b85b78
  expiration: 86400000
eureka:
  instance:
    hostname: localhost
    prefer-ip-address: false
    instance-id: ${spring.application.name}:${server.port}
    status-page-url: http://localhost:${server.port}/actuator/info
    health-check-url: http://localhost:${server.port}/actuator/health
server:
  port: 8084
